

全量发布文件列表：runtimes文件夹，IotPlatform.runtimeconfig.json ，IotPlatform.exe，*.dll

表单数据源
表单工作流
门户模块功能：保存、编辑、删除、通过系统id查看门户、发布、撤销发布
通过自然语言描述自动调用相关功能创建表单、数据库表等功能
表单SQL报错问题处理："42601: syntax error at or near \"end\"\n\nPOSITION【原因：sql字段名不能用end这种保留关键字】
API编辑器增加复制接口
确认自定义文件夹上传逻辑是否正常
文件上传自定义文件夹，上传后无法正常展示：代码中上传参数folder没有处理
文件上传后没有产即生成fileId：finleName就是fileId
生产计划排程：IotPlatform工程结构；子项目名称修改（引入公共契约）；
生产计划排程：交期优先法、设备利用率最大化（适合换线成本高，如换模）、瓶颈资源优先
生产计划排程：输出的JSON要求新增计划排产时间字段（apsStartTime与原字段名区分开来）
实现分钟级的紧急插单重排
插件管理模块及排程算法插件
数据库管理工具开发
排产问题：非(待排产)工单排产后出现开始时间和结束时间一样【已解决】
排产问题：非(待排产)工单排产后出现两个工单在一台设备上同时生产，时间重叠了。需要重构，需要将基准时间拆开，分为TimelineOrigin（统一时间原点）和OptimizationCutoff（排产优化起点）
排产需求：增加异常占用区间，就是标记了某台设备或者某个模具  在starttime和endtime这段时间内  是没办法生产的，排产的时候不要把工单排到这个时间段
表单->创建数据接口：解决【计算公式】类型字段保存时报错“column is of type numeric but expression is of type text”的问题
数据应用->数据接口：“关键字”可以按uuid查数据

发布表单->提示【无表转有表失败】：数据库不支持自增主键，暂时没有打印异常日志，后续需要加上  todo xzy
多实例部署：文件管理、雪花id配置机器码、插件(分布式锁、分布式缓存)，底层数据库切换Lock锁





2026.8.17 上午10:10分机票，中午到达贵阳，办理入住放下行李，下午去了全丰名剑工厂，了解基本情况，配置网络以便服务器远程
整理验收文档，记录发现的问题，晚上和吴工一起确认问题并确定解决方案
整理验收文档，系统截图，按PDF标准输出文档集合，晚上吴工给我们培训系统功能，和维拓开会（了解项目他们已有验收项目失败案例）
解决表单问题（1.在下发前增加校验；2.存储字段和显示字段不一样的表单数据保存后不能正常显示的问题，排查和解决），熟悉系统功能，整理基础数据并记录问题，导入安航组织数据和用户数据。
上午到全丰名剑工厂，与客户沟通基础数据；另外，由于政府的验收标准是一个功能点不合格则整个模块都不合格，所以我们需要沟通保留哪些真正能用起来的功能，砍掉的用不起来的功能模块。下午导基本数据

安航是否有工节路线附件？



2026.8.7
本周工作：
  1.完成DbAdmin的开发及所有接口的基本功能测试，可以交给前端对接；目前仅测试PostgreSql，后端还需详细测试复杂接口的各种特殊情况、性能、MySql等，
  2.解决实施反馈的【计算公式】类型字段保存时报错问题，column is of type numeric but expression is of type text
下周计划：
  详细测试MySql和复杂接口的各种特殊情况，如表结构修改时将字符串转成int、导入数据需要测试不同的数据类型和导入模式、查询数据需要验证不同操作符、大数据量导入导出性能等
  底层数据库方言源码

DbAdmin接口已测试完成，目前仅测试了Mysql库和PostgreSql库，其他库暂不支持；
大批量数据方面，造了100万行测试数据（8个字段），测试了两种库的导入导出，导入100d万数据，Mysql大概在160多秒左右，PostgreSql有时候160多秒有时候360多秒（CSV格式最快104秒）。导出100万数据，MySql和PostgreSql导出EXCEL需要4分钟左右。
代码里目前先限制导入导出最多只能200万行，导入文件限制200M，够吗？

数据库管理工具：
1.表数据导入功能已完成优化，优化后百万数据导入PostgreSql用时(xlsx文件225秒，csv文件79秒）, 百万数据导入Mysql用时（xlsx文件157秒，csv文件97秒）；通过Navacat将百万数据导入Mysql用时（xlsx文件260秒，csv文件249秒）
2.目前表数据导出用时与Navicat差不多，暂不优化

DbAdmin已发布120测试服务器，发布时间2026年8月13日10:44:41

2026.7.31
本周工作：
  排产算法优化：（1）指定模具不会被排到不配对设备 （2）处理动态作业与固定作业之间的过渡关系，确保它们在同一机器上时满足换模准备时间约束。
  DbAdmin开发：完成工程结构调整、表结构设计服务、元数据查询服务、表数据服务，表数据导入导出服务相关接口功能，完成这些接口功能的代码分析、代码逻辑优化、风险排查、性能排查、复用并重构通用逻辑、审计日志等。大致看了下由AI生成底层数据库方言源码，后期还需要详细阅读和分析。其中"数据导入导出服务"完成70%左右。
下周计划：
  SQL控制台服务相关接口
  完善导入导出接口性能排查、优化、临时文件
  其他接口：创建数据库、生成表模板SQL、实体管理页面相关接口、结合前端功能页面排查缺少的接口并补全
  接口分类及代码结构调整，完善注释、swagger注释、log日志、error日志、审计日志
  测试



