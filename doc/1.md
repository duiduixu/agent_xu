


YesSql 是一个基于 .NET 平台的文档数据库接口（或称文档数据库抽象层）。它的核心作用是让开发者能够在使用纯关系型数据库（RDBMS）的同时，享用类似文档数据库（NoSQL）的灵活性和 API 操作方式。
Activity模型（活动图）与BPMN模型（业务流程模型和标记法）是常用于描述工作流和业务逻辑的两种图形化建模语言。【Activity模型（UML活动图）背景：属于统一建模语言（UML）的一部分，主要面向软件开发人员和系统架构师。】【BPMN模型（Business Process Model and Notation）背景：由OMG组织维护的独立标准（主流为BPMN 2.0），专门面向业务分析师、运营人员及流程自动化专家。】
启动Codex时提示SKILL"missing YAML frontmatter delimited by ---"，问题原因是文件最开头有不可见的UTF-8 BOM，另存为“UTF-8（无 BOM）”即可。
SSE（Server-Sent Events） 是一种基于 HTTP 的服务器向客户端单向推送技术。SSE 建立在普通 HTTP 协议之上。SSE 使用一种简单文本协议。
协调世界时（Coordinated Universal Time，简称UTC），又称世界统一时间或祖鲁时间（Zulu Time），是当今全球的官方民用时间标准与所有时区划分的绝对参考系。
数据库中的 Identity列（标识列） 和 主键列（Primary Key） 是两个不同的概念，Identity列负责自动生成递增的数字，而主键列负责唯一标识每一行数据并确保不重复。在实际开发中，它们经常结合在一起使用，即把自动递增的Identity列同时设为主键。一个表只能有一个Identity列，且数据类型必须是整数（如 int、bigint）。
CLOB 是一种字符大型对象（Character Large Object）数据库数据类型，主要用于存储大容量的文本数据。
await Task.Yield() 的作用是强制将当前执行线程切回到线程池，并主动让出 CPU 时间片。它告诉调度器："我现在可以暂停，让其他任务先执行"。
TTS 是 Text-to-Speech 的缩写，中文称为“语音合成”或“文语转换”‌，是一种将书面文本转换为自然流畅语音输出的技术 。它让计算机能够模仿人类说话，将文字信息转化为可听的声音 。
Monaco Editor 是微软开发并开源的网页端代码编辑器，也是 VS Code 的核心支撑部分。它具备强大的语法高亮、智能代码补全、代码折叠和错误提示等功能，常用于网页端实现专业级代码编辑体验。
NG是 No Good 的缩写，意思是“不好”或“不行”【工厂与商品：指产品检测没有通过的不良品、残次品。】
MySqlBulkLoader（如 .NET 中的 MySqlBulkLoader 类）向 MySQL 导入数据时，底层执行的就是 LOAD DATA LOCAL INFILE 命令。该功能要求 服务器端 和 客户端/驱动连接端 必须同时启用 local_infile，否则会直接抛出类似 Loading local data is disabled 的错误。【服务器端（Server）：MySQL 服务端全局变量 local_infile 必须设置为 ON（或 1）。客户端/驱动连接端（Client/Driver）：代码中的连接字符串或配置必须显式允许加载本地文件（例如在连接字符串中设置 AllowLoadLocalInfile=true，或在 MySqlBulkLoader 中设置 Local = true）。】
临时表：MySQL：临时表生命周期 = connection/session 生命周期，PostgreSQL：默认也是 session 生命周期，但可以通过 ON COMMIT 变成 transaction 生命周期。两者断开连接都会自动回收。使用连接池时，不要假设“请求结束=临时表删除”
NpgsqlTransaction ---> System.OperationCanceledException: Query was cancelled at Npgsql.Internal.NpgsqlReadBuffer.HTTP。这是一个 Npgsql（.NET PostgreSQL 驱动）在执行数据库写入时被取消，导致事务对象被释放后继续使用的异常。请求超时，ASP.NET Core 请求被客户端断开，BackgroundService 停止，手动调用 CancellationTokenSource.Cancel()，超时策略触发，Npgsql 会收到取消信号：System.OperationCanceledException: Query was cancelled，然后 COPY 事务失败。

supervisorctl
dotnet code,  .net6使用 Minimal APIs 架构（按模块/路由组控制）同时控制入参和出参Json大小驼峰规则，var userModule = app.MapGroup("/api/v1/users");// 先定义一个特定的模块路由组，再配置路由组规则为小驼峰userModule.WithJsonOptions(options => {  options.JsonSerializerOptions.PropertyNamingPolicy = JsonNamingPolicy.CamelCase;});
dotnet code，  $"{abnormalGroupId}#{index:D4}";   index 这个数字会被格式化为至少 4 位数字，不足 4 位时在左边补零。对于浮点数，应使用 N、F 等格式，例如 {value:F2}（保留两位小数）
dotnet code，SelectMany用于将集合扁平化
dotnet code, x!Value的作用是告诉编译器"这个表达式不会为 null，请停止警告"。
irm 是 PowerShell 的内置命令（别名），全称为 Invoke-RestMethod。它主要用于向 Web 服务器发送 HTTP/HTTPS 请求并获取数据（如 JSON、文本），常用于快速下载远程脚本或 API 交互。
dotnet restore .\IotPlatform.sln
dotnet build .\03-应用服务\IotPlatform\IotPlatform.csproj -c Release
dotnet publish .\03-应用服务\IotPlatform\IotPlatform.csproj -c Release -o d:\tmp\publish\cloudiot
mkdir -p /app/publish_tmp
rm -rf /app/publish_tmp/cloudiot
unzip -o /app/cloudiot_publish.zip -d /app/publish_tmp

cp -a /app/cloudiot /app/cloudiot_bak_$(date +%Y%m%d_%H%M%S)
cp -rf 原文件夹路径/* 目标文件夹路径/   其中 -r 代表递归复制（把文件夹和里面的文件一起复制），-f 代表强制覆盖（如果文件同名，不询问直接覆盖）。  /*：代表复制该文件夹下的所有内容，不包含文件夹本身。

docker stop cloudiotv1

cp -a /app/publish_tmp/cloudiot/. /app/cloudiot/

docker start cloudiotv1

docker logs -f --tail 200 cloudiotv1

通过端口确认进程netstat -nlp | grep 9004
获取部署的实际物理路径ls -l /proc/<PID>/cwd
查看启动详情和环境变量ps -ef | grep <PID>
查看nginx配置，nginx -t
grep -rn "9004" /etc/nginx/
查看nginx的包含的独立配置文件cat /etc/nginx/nginx.conf | grep include
        Console.WriteLine($"当前驱动器: {Directory.GetCurrentDirectory()[0]}:");//包含当前工作目录绝对路径且结尾不带有反斜杠（\）的字符串。
        Console.WriteLine($"完整路径: {Path.Combine(directoryPath, name)}");

dotnet js执行引擎，Jint 是一个完全用纯 C# 编写的 ECMAScript 解释器。ClearScript 是由微软维护的开源库，它允许.NET应用无缝集成高性能的脚本引擎【底层采用 Google 的 V8 引擎（与 Chrome 和 Node.js 相同）】。
DBeaver Community is a free, open-source database management tool recommended for personal projects ,Manage and explore SQL databases like MySQL, MariaDB




NPOI作为基于POI的开源C#库，提供无需Office环境的Word/Excel读写能力，支持xls/xlsx/docx格
SqlSugar：ExecuteCommandAsync 方法用于异步执行一个 SQL 命令，例如 INSERT, UPDATE, DELETE 等。这个方法主要用于执行那些不需要返回受影响行数的操作。ExecuteCommandHasChangeAsync 方法同样用于异步执行 SQL 命令，但它返回一个布尔值，指示命令执行后是否有行被改变。
“回包”在计算机术语中是指服务器收到你的请求后，直接把数据放到网页或软件界面上显示给你（即 HTTP 返回数据包）。
OLTP（联机事务处理Online Transaction Processing）是用来记录日常交易的系统（如在超市结账）。OLAP（联机分析处理Online Analytical Processing）是用来查看报表和找规律的系统（如经理看上个月哪种商品卖得最好）。它们是现代企业管理数据不可缺少的两部分。
openGauss是一款由华为推出的企业级开源关系型数据库。
KingbaseES（金仓数据库管理系统）是一款由中国电科金仓（原人大金仓）自主研发的企业级大型通用关系型数据库。它常用于需要处理海量数据、高并发和高可用性的关键业务场景。基于开源的 PostgreSQL 架构进行了深度自研和内核扩展，具备优秀的开源生态兼容性。尤其在多生态兼容和平滑扩展方面表现出色，能够轻松实现企业级核心业务的支撑。
达梦（DM）：前身是华中科技大学数据库研究所（由冯裕才教授创立），现为中国电子信息产业集团（CEC）旗下企业。是国内最早从事数据库管理系统研发的科研机构之一。采用完全自主的原创内核（从底层存储、SQL解析到执行器均为自研）。在架构设计上，其商业理念非常对标国际巨头 Oracle。
Chat2DB OtterMind/Chat2DB(GitHub)： 阿里开源的 AI 驱动数据库管理工具。
Claude CLI会话使用技巧：在同一个会话中进行大量生成操作，导致 Claude CLI 自动开启 compact（压缩/精简模式）并产生巨大的 token 消耗，主要是由于 上下文窗口膨胀（Context Window Bloat） 引起的。 优化建议：如何避免再次出现此问题？1.适时清理会话（使用 /clear 或开启新会话）当任务切换，或者完成了某阶段的分析文档后，不要在一个会话里无休止地聊下去。这不仅消耗 Token，还会因为历史信息过多导致 AI 的回答质量下降。2.利用总结沉淀知识（提效降本黑科技）在完成重要文档分析后，命令 Claude 把当前状态总结并保存为一个独立的 Markdown 文件（例如 report.md）。然后开启新会话，通过 @report.md 快速导入总结。这样既保持了完整的上下文，又清空了之前冗长的历史对话负担。
Claude CLI：查看当前会话的上下文分配/context    /usage     /cost    /compact，将之前的聊天记录进行总结和压缩，释放宝贵的上下文空间。    /clear完全清空对话历史
codex cli: /status   /compact     /fork派生新会话（保留历史，重启额度）
/context作用：显示当前会话中各种内容（系统提示词、工具、外部文件、MCP服务器等）对上下文窗口的占用详情，帮助您直观了解哪些内容在消耗空间。
/usage作用：显示您当前配额的使用比例（针对5小时限制或每周限制）、距离额度重置的时间，以及各插件/子Agent的消耗详情
使用 Apifox 的“前置操作”功能。脚本会在发送请求前，自动过滤掉所有值为空的 Query 参数。
C# 9.0 的 with 表达式（hint with { ... }），这是一种不可变对象的复制修改语法：创建 hint 的副本，修改副本的 Status 属性，返回修改后的副本，这种方式避免了直接修改原对象，保持了代码的不可变性和线程安全性。
Linq 排序方法OrderByDescending，布尔值的排序规则：true = 1，false = 0
，所以 OrderByDescending 会让 true 的项排在前面。
Access-Control-Allow-Origin  告知浏览器哪些源（域名/协议/端口组合）被允许访问当前资源 
数据库事创建表和插入数据不可以在同一个传统事务中，因为在大多数主流数据库（如 MySQL、Oracle）中，执行像 CREATE TABLE 这样的 DDL（数据定义语言）语句会隐式提交（Implicit Commit）当前事务。
数据提交的事务：SqlSugarUnitOfWork 是对 Furion IUnitOfWork 的具体实现（位于 Extras.DatabaseAccessor.SqlSugar 核心包下）：
1.AOP 拦截介入：由于 VisualDevService 实现了 IDynamicApiController（Furion 动态 Web API 接口），该请求会被 Furion 框架内置的 UnitOfWorkFilter 过滤器拦截。
2.事务自动开启：Action 执行前，框架调用 SqlSugarUnitOfWork.BeginTransaction，内部通过 _sqlSugarClient.AsTenant().BeginTran() 在当前租户数据库连接上下文中开启事务。
3.数据操作执行：
4.异常发生时回滚：如果验证失败或后续 SyncField 出错，抛出 Oops.Oh 异常。UnitOfWorkFilter 捕获到异常后，调用 RollbackTransaction，执行 RollbackTran() 回滚当前线程挂起的所有未保存更改，保证数据库不被污染。
5.成功时自动提交：若整个 Create 方法执行顺利无异常，Action 执行完毕后程序流经 CommitTransaction 触发 CommitTran() 提交，并完成真实物理落库。
